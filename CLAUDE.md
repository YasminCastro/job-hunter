## Execução

- **Nunca execute `main.py` (ou qualquer script de scraping) automaticamente.** O usuário roda localmente de propósito, para evitar que o LinkedIn identifique execuções repetidas vindas de um IP de datacenter/CI. Apenas edite o código; não rode para "testar".
